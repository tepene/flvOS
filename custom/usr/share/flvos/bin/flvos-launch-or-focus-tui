#!/usr/bin/env bash

## source: https://github.com/basecamp/omarchy/blob/dev/bin/omarchy-launch-or-focus-tui

# Launch or focus on a given TUI identified by the passed in as the command.
# Use by commands like omarchy-launch-wifi to ensure there is only one wifi configuration screen open.

APP_ID="org.flvos.$(basename "$1")"
LAUNCH_COMMAND="flvos-launch-tui $@"

exec flvos-launch-or-focus "$APP_ID" "$LAUNCH_COMMAND"
